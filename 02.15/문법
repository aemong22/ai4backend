Math.floor()
: 소수점 이하를 버림한다.

Math.ceil() 
: 소수점 이하를 올림한다.

Math.round() 
: 소수점 이하를 반올림한다.

parseInt 
: 문자열을 정수로 변환한다.


#배열의 내장 메서드
kname.length
: 길이

kname.push("예빈")
: "예빈"을 맨뒤에 추가

kname.indexOf("예빈")
: "예빈"의 위치 반환 

kname.findIndex(f => f==="예빈")
: 얘는 콜벡함수를 사용하여 위치 반환

kname.reverse()
:리스트 뒤집기
:kname은 자체가 뒤집어진다.

kname.sort()
: 정렬

kname.pop()
: 맨마지막꺼 반환하고 리스트에서는 삭제


# 문자열 메서드
let first = "가나다라마바사"

first.charAt(3)
: (괄호안 숫자) 위치에 있는 문자열 하나 반환
: 라


# 데이터 import /export
ES6 : import / export 
:package.json에서 "type": "module" 추가해야한다.

Node.js : requre/ exports
: 노드는 객체로 내보낸다. => {  }
:package.json에서 "type": "module" 삭제해야한다.

toString() 
:객체가 가지고 있는 정보나 값들을 문자열로 만들어 리턴

slice(0,10)
: 문자열 자르기


# 노드의 강점
node의 강점 : 비동기식. asyncronous
노드는 기본적으로 싱글 스레드다.
